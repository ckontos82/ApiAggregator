namespace ApiAggregator.Features.Aggregation.Enums
{
    /// <summary>
    /// Direction the merged results are sorted in.
    /// </summary>
    public enum SortDirection
    {
        /// <summary>Smallest or earliest first.</summary>
        Ascending = 1,

        /// <summary>Largest or latest first.</summary>
        Descending = 2
    }
}
